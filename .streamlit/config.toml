# Local `make ui` can still pass --server.port; do not hardcode a port here —
# Streamlit Cloud health-checks 8501 and will fail if this file forces 8502.

[browser]
gatherUsageStats = false

[server]
headless = true
enableCORS = false
enableXsrfProtection = false
